@model IEnumerable<Managerhotel.Models.Massagesalon>

@{
    ViewBag.Title = "سالن ماساژ";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Attendance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlanWoman)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlanMan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Massagelist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Attendance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlanWoman)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlanMan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Massagelist)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MassagesalonId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MassagesalonId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MassagesalonId })
        </td>
    </tr>
}

</table>
