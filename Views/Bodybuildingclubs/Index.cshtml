@model IEnumerable<Managerhotel.Models.Bodybuildingclub>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Attendance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlanWoman)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlanMan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Attendance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlanWoman)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlanMan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BodybuildingclubId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BodybuildingclubId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BodybuildingclubId })
        </td>
    </tr>
}

</table>
